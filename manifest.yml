declared-services:
  wga-assistant:
    label: conversation
    plan: free
  wga-redis_
    label: rediscloud
    plan: 30mb
applications:
- memory: 256M
  random-route: false
  services:
  - wga-assistant
  - wga-redis
